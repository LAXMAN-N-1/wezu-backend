apiVersion: v1
kind: Service
metadata:
  name: wezu-backend-v2
  labels:
    app: wezu-backend-v2
spec:
  selector:
    app: wezu-backend-v2
  ports:
    - name: http
      port: 80
      targetPort: 8081
  type: ClusterIP
