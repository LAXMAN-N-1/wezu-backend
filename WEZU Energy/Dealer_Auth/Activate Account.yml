info:
  name: Activate Account
  type: http
  seq: 7
  tags:
    - Dealer_Auth

http:
  method: POST
  url: "{{baseUrl}}/api/v1/dealer/auth/activate/:token"
  params:
    - name: token
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "password": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/dealer/auth/activate/:token"
      method: POST
      params:
        - name: token
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/api/v1/dealer/auth/activate/:token"
      method: POST
      params:
        - name: token
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "password": ""
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }

docs: Activate a pending account using the invite token — sets password and logs in.
