info:
  name: Get Trends
  type: http
  seq: 2
  tags:
    - Dealer_Analytics

http:
  method: GET
  url: "{{baseUrl}}/api/v1/dealer/analytics/trends"
  params:
    - name: period
      value: daily
      type: query
      description: daily, weekly, or monthly
    - name: periods
      value: "7"
      type: query
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: /api/v1/auth/token
    refreshTokenUrl: "{{oauth_refresh_url}}"
    credentials:
      clientId: "{{oauth_client_id}}"
      clientSecret: "{{oauth_client_secret}}"
      placement: header
    tokenConfig:
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: false
      autoRefreshToken: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/dealer/analytics/trends"
      method: GET
      params:
        - name: period
          value: daily
          type: query
          description: daily, weekly, or monthly
        - name: periods
          value: "7"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/api/v1/dealer/analytics/trends"
      method: GET
      params:
        - name: period
          value: daily
          type: query
          description: daily, weekly, or monthly
        - name: periods
          value: "7"
          type: query
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }

docs: Revenue + swap volume trends for the last N periods.
