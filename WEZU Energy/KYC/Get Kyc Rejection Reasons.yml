info:
  name: Get Kyc Rejection Reasons
  type: http
  seq: 8
  tags:
    - KYC

http:
  method: GET
  url: "{{baseUrl}}/api/v1/rejection-reasons"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/rejection-reasons"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "code": "",
              "description": ""
            }
          ]

docs: Fetch available standardized rejection reason codes
