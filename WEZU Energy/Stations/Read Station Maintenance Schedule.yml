info:
  name: Read Station Maintenance Schedule
  type: http
  seq: 12
  tags:
    - Stations

http:
  method: GET
  url: "{{baseUrl}}/api/v1/stations/:station_id/maintenance-schedule"
  params:
    - name: station_id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/stations/:station_id/maintenance-schedule"
      method: GET
      params:
        - name: station_id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/api/v1/stations/:station_id/maintenance-schedule"
      method: GET
      params:
        - name: station_id
          value: ""
          type: path
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }

docs: View upcoming and past maintenance for station.
