info:
  name: Create Reorder Request
  type: http
  seq: 73
  tags:
    - Admin_Core
    - Admin_Stock_Levels

http:
  method: POST
  url: "{{baseUrl}}/api/v1/admin/stock/reorder"
  body:
    type: json
    data: |-
      {
        "station_id": 0,
        "requested_quantity": 0,
        "reason": ""
      }
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: /api/v1/auth/token
    refreshTokenUrl: "{{oauth_refresh_url}}"
    credentials:
      clientId: "{{oauth_client_id}}"
      clientSecret: "{{oauth_client_secret}}"
      placement: header
    tokenConfig:
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: false
      autoRefreshToken: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/admin/stock/reorder"
      method: POST
      body:
        type: json
        data: |-
          {
            "station_id": 0,
            "requested_quantity": 0,
            "reason": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "",
            "station_id": 0,
            "requested_quantity": 0,
            "reason": "",
            "status": "",
            "created_at": ""
          }
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/api/v1/admin/stock/reorder"
      method: POST
      body:
        type: json
        data: |-
          {
            "station_id": 0,
            "requested_quantity": 0,
            "reason": ""
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }

docs: Create a reorder request for a station (FR-ADMIN-INV-003)
