info:
  name: List Security Questions
  type: http
  seq: 24
  tags:
    - Auth

http:
  method: GET
  url: "{{baseUrl}}/api/v1/auth/security-questions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/auth/security-questions"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          [
            {
              "id": 0,
              "question_text": ""
            }
          ]

docs: Fetch list of available security questions
