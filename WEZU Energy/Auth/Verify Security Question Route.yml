info:
  name: Verify Security Question Route
  type: http
  seq: 26
  tags:
    - Auth

http:
  method: POST
  url: "{{baseUrl}}/api/v1/auth/security-questions/verify"
  params:
    - name: user_id
      value: ""
      type: query
  body:
    type: json
    data: |-
      {
        "answer": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Successful Response
    request:
      url: "{{baseUrl}}/api/v1/auth/security-questions/verify"
      method: POST
      params:
        - name: user_id
          value: ""
          type: query
      body:
        type: json
        data: |-
          {
            "answer": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: ""
  - name: 422 Response
    description: Validation Error
    request:
      url: "{{baseUrl}}/api/v1/auth/security-questions/verify"
      method: POST
      params:
        - name: user_id
          value: ""
          type: query
      body:
        type: json
        data: |-
          {
            "answer": ""
          }
    response:
      status: 422
      statusText: Unprocessable Entity
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "detail": [
              {
                "loc": [],
                "msg": "",
                "type": ""
              }
            ]
          }

docs: Verify security question during recovery
